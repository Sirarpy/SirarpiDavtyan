{
  "name": "portfolio",
  "version": "1.0.0",
  "dependencies": {
    "animated-scroll-to": "^2.0.5"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  },
  "scripts": {
"predeploy": "npm run build",
"deploy": "gh-pages -d build"
}
}
